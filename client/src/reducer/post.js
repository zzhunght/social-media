export const PostReducer = (state, action) =>{
    const {type,payload} = action;

    switch(type){
        case 'SET_POST':
            return state
        default:
            return state
    }
}